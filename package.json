{
  "name": "ts_react_python_electron",
  "version": "0.0.1",
  "description": "",
  "main": ".\\dist\\main.js",
  "private": true,
  "scripts": {
    "build": "electron-packager ./ openDB_downloader --platfrom=darwin --arch=x64 --overwrite --out=./build",
    "start": "concurrently \"tsc -w\" \"electron .\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "electron": "^22.0.0",
    "electron-packager": "^17.1.1",
    "electron-reload": "^2.0.0-alpha.1",
    "python-shell": "^3.0.1",
    "typescript": "^4.9.4",
    "vue": "^3.2.45"
  }
}
